{
  "name": "orbis",
  "module": "main.ts",
  "type": "module",
  "workspaces": [
    "core/*",
    "apps/*"
  ],
  "devDependencies": {
    "@modelcontextprotocol/inspector": "^0.21.1",
    "@types/bun": "latest",
    "typescript": "^5"
  },
  "dependencies": {},
  "scripts": {
    "orbis": "bun main.ts",
    "setup": "bun scripts/setup.ts",
    "test": "bun test test-master.test.ts && bun scripts/cleanup-tests.ts",
    "build": "bun run --filter \"*\" build",
    "typecheck": "bun x tsc --project tsconfig.base.json",
    "clean": "bun scripts/cleanup-tests.ts",
    "mcp": "bun run --cwd core/mcp start",
    "mcp-inspector": "bunx @modelcontextprotocol/inspector bun run --cwd core/mcp start",
    "dashboard": "bun run --cwd apps/dashboard dev"
  }
}
